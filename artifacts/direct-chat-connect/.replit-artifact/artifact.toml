kind = "web"
previewPath = "/"
title = "Direct Chat Connect"
version = "1.0.0"
id = "artifacts/direct-chat-connect"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19714

[services.development]
run = "pnpm --filter @workspace/direct-chat-connect run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/direct-chat-connect", "run", "build" ]
publicDir = "artifacts/direct-chat-connect/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19714"
BASE_PATH = "/"
